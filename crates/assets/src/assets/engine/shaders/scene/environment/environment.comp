#version 460 core
layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0, rgba32f) writeonly uniform image2DArray enviro;

void main() {
	vec4 data = vec4(1);
	//imageStore(enviro, ivec3(gl_GlobalInvocationID.xyz), data);
}