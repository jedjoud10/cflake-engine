// An event handler will keep track of calling back the registered events and shit
// The event handler will live alongside the world
pub struct EventHandler {

}

// This is a window event that will be triggerred from glutin
pub type WindowEvent;

// This is a device event that will be triggerred from glutin
