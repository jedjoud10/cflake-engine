use world::{System, World};

// Pre-generate some chunks into the world
fn init(world: &mut World) {
}

// TODO: Dynamically generate the chunks based on camera position
fn update(world: &mut World) {  
}

// Adds/removes the chunk entities from the world
pub fn manager(system: &mut System) {
}
