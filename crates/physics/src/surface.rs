// A surface type that can be applied whenever we create a new collider
pub struct Surface {
    // The bouncy-ness of a surface
    pub restitution: f32,
    // The friction of a surface
    pub friction: f32,
}
