#[cfg(test)]
mod tests {
    use assets::Asset;

    use crate::Mesh;

    #[test]
    fn test() {
        let test: assets::loader::AssetLoader = todo!();

        //let mesh = Mesh::try_load
    }
}
