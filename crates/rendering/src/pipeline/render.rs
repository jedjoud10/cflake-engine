use graphics::{GraphicsPipeline, vk, Graphics};
use world::World;
use crate::Material;

// Render all the visible surfaces of a specific material type
pub(super) fn render_surfaces<M: Material>(
    pipeline: &GraphicsPipeline
) {
}