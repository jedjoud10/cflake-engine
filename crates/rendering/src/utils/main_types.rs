// Simple main OpenGL types
#[derive(Clone, Copy)]
pub enum DataType {
    // 8 bit
    UByte,
    Byte,
    // 16 bit
    UInt16,
    Int16,
    // 32 bit
    UInt32,
    Int32,
    // FP
    Float32,
}
