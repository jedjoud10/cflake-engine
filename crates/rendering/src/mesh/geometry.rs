// A geometry object that contains some material and the actual vertices and triangles
pub struct Geometry {
    // Vertex array object that combines all of the attributes and the EBO together
    vao: u32,

    // Vertex attributes
    positions: Buffer<

    // Indices
}