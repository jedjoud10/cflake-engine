// A mesh that contains multiple geometries of different materials
pub struct Mesh {
    geometries: Vec<Geometry>
}

impl Mesh {
    // Create a mesh using multiple geometries
    pub fn new(geometries: Vec<Geometry>, _ctx: &crate::Context) -> Self {

    }
}