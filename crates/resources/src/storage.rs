// A storage simply contains multiple values of the same type
// These values can then be acessed using handles. If a value has no handles, it will automatically be de-allocated and removed from the storage
pub struct Storage<T> {
    
}