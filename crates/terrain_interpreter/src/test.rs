// Testing time
#[cfg(test)]
mod test {
    use crate::Interpreter;

    #[test]
    pub fn nodes() {
        // Create the interpreter system
        let interpreter = Interpreter::default();
        
    }
}