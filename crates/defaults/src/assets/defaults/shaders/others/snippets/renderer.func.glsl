// Load the default uniforms that would normally be used when dealing with renderers
// Global renderer uniforms
uniform mat4 _model_matrix;
uniform mat4 _pv_matrix;
uniform vec2 _nf_planes;
uniform vec3 _cam_dir;
uniform vec3 _cam_pos;