// The global eGUI mesh that we will draw onto the screen.
//Sadly, we can't separate multiple elements into multiple batches, since all the vertices and indices are stored within the same lists
pub struct Mesh {}
